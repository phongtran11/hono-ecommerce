info:
  name: Get Single Product
  type: http
  seq: 4

http:
  method: get
  url: "{{base_url}}/api/products/:id"
  auth: inherit

vars:
  preRequest:
    - name: id
      value: "product-uuid-here"

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
