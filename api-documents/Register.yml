info:
  name: Register
  type: http
  seq: 1

http:
  method: post
  url: "{{base_url}}/api/auth/register"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "user1@example.com",
        "password": "secret123",
        "name": "Test User"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
