info:
  name: List Orders
  type: http
  seq: 11

http:
  method: get
  url: "{{base_url}}/api/orders"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
