info:
  name: Remove Cart Item
  type: http
  seq: 8

http:
  method: delete
  url: "{{base_url}}/api/cart/:itemId"
  auth: inherit

vars:
  preRequest:
    - name: itemId
      value: "item-uuid"

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
