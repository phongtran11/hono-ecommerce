info:
  name: Get Single Order
  type: http
  seq: 12

http:
  method: get
  url: "{{base_url}}/api/orders/:id"
  auth: inherit

vars:
  preRequest:
    - name: id
      value: "order-uuid"

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
